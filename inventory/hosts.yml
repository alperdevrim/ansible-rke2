---
# RKE2 Cluster Inventory
# Update the IP addresses below with your actual node IPs

all:
  children:
    rke2_cluster:
      children:
        rke2_servers:
          hosts:
            master1:
              ansible_host: 192.168.1.211
              ansible_user: master1
              ansible_ssh_pass: root
              ansible_become_pass: root
        rke2_agents:
          hosts:
            worker1:
              ansible_host: 192.168.1.212
              ansible_user: worker1
              ansible_ssh_pass: root
              ansible_become_pass: root
            worker2:
              ansible_host: 192.168.1.217
              ansible_user: worker2
              ansible_ssh_pass: root
              ansible_become_pass: root

  vars:
    # Common SSH settings
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"

